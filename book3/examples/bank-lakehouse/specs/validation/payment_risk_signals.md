# Проверочные факты: mart_payment_risk_signals

## Автоматические факты

- `dbt build --profiles-dir .` завершается с кодом 0.
- `transaction_id` проходит `not_null` и `unique`.
- `risk_signal` проходит `not_null`.
- `assert_payment_risk_signal_values` возвращает 0 строк.

## Ручные факты

- Ревьюер подтверждает, что эта витрина является списком сигналов, а не
  боевым решением скоринга.
- Ревьюер подтверждает, что grain уровня операции не заменён агрегацией по
  клиенту.
